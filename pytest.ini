[pytest]
markers =
    gui: tests that require GUI dependencies like dearpygui
    integration: slow or integration-style tests (kept separate in CI)

# Docker dev cannot write to home dir, so we set a temp cache dir
cache_dir = /tmp/pytest_cache
